import 'package:flutter/material.dart';

final ThemeData lightTheme = ThemeData(
  brightness: Brightness.light,
  primaryColor: const Color.fromARGB(255, 255, 255, 255),
  scaffoldBackgroundColor: Color.fromARGB(255, 202, 209, 228),
  colorScheme: ColorScheme.light(
    
    
  )


  // Define additional light theme properties here
);final ThemeData darkTheme = ThemeData(
  brightness: Brightness.dark,
  primaryColor: Color(0xFF202124),
  scaffoldBackgroundColor: Color(0xFF202124),
  
  // Define additional dark theme properties here
);